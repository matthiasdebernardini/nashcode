[package]
name = "dgit-index"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Parse the repository list off a dgit server's HTML index page."

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
